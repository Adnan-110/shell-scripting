#!/bin/bash

read -p "Enter your name:" name   #its used take input from user
echo "Name of the person is $name"

# Here read statement which is enclosed with " " will be print and ask for user input and user input will be stored in name variable
#In next line we use echo cmd to print the name variable
